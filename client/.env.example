# Google Maps JavaScript API (required for dealer locator & map picker)
# Create a key at https://console.cloud.google.com/ — enable "Maps JavaScript API"
# Restrict key by HTTP referrer in production.
VITE_GOOGLE_MAPS_API_KEY=
